'use client';
import Link from 'next/link';
import {useState} from 'react';
import LafreShell from '../../prototype/components/LafreShell';
import ui from '../../prototype/components/ui.module.css';
export default function AiTool({params}){const [run,setRun]=useState(false);const name=params.slug.split('-').map(x=>x[0].toUpperCase()+x.slice(1)).join(' ');return <LafreShell active="AI Tools"><div className={ui.shell}><Link href="/ai-tools" className={ui.muted} style={{textDecoration:'none',fontWeight:800}}>← AI Tools</Link><div className={ui.panel+' '+ui.pad} style={{marginTop:15,maxWidth:900}}><div className={ui.eyebrow}>Mock AI workspace</div><h1 style={{fontSize:29,margin:'8px 0'}}>{name}</h1><p className={ui.muted}>This is a visual prototype. The real version will call the AI service and show structured results here.</p><textarea placeholder="Paste text or describe what you want the tool to do..." style={{width:'100%',minHeight:210,border:'1px solid #d9d5cc',borderRadius:8,padding:13,marginTop:12}}/><div className={ui.row} style={{marginTop:11}}><span className={ui.muted} style={{fontSize:11}}>No document is uploaded in this prototype.</span><button className={ui.goldBtn} onClick={()=>setRun(true)}>Run tool</button></div></div>{run&&<div className={ui.panel+' '+ui.pad} style={{marginTop:15,maxWidth:900}}><div className={ui.eyebrow}>AI result · demo</div><h3>General observations</h3><ul style={{lineHeight:1.8,color:'#4d4941'}}><li>This result is simulated frontend data.</li><li>The production tool will explain its reasoning and limitations.</li><li>Users should be encouraged to seek a verified lawyer for specific advice.</li></ul><div className={ui.tag}>Mock API response</div></div>}</div></LafreShell>}
