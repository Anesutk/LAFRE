'use client';
import {useState} from 'react';
import LafreShell from '../prototype/components/LafreShell';
import LawyerCard from '../prototype/components/LawyerCard';
import {lawyers} from '../prototype/mock-api';
import ui from '../prototype/components/ui.module.css';
export default function Connections(){const [tab,setTab]=useState('My connections'); return <LafreShell active="Connections"><div className={ui.shell}><div className={ui.eyebrow}>Professional network</div><h1 className={ui.title}>Connections</h1><p className={ui.subtitle}>A separate networking layer from the public forum. Requests can be accepted or declined before private communication is enabled.</p><div style={{display:'flex',gap:8,margin:'18px 0'}}>{['My connections','Requests','Discover'].map(x=><button key={x} onClick={()=>setTab(x)} className={x===tab?ui.goldBtn:ui.outlineBtn}>{x}</button>)}</div>{tab==='Discover'?<div className={ui.threeCol}>{lawyers.map(l=><LawyerCard key={l.slug} lawyer={l} compact/>)}</div>:<div className={ui.panel+' '+ui.pad}>{tab==='Requests'?<><h2 style={{fontSize:19}}>Pending requests</h2>{['Munashe P. wants to connect with you.','Rudo K. invited you to professional discussion.'].map((x,i)=><div className={ui.listItem} key={i}><div className={ui.row}><div><b>{x}</b><div className={ui.muted} style={{fontSize:11,marginTop:4}}>Received today · mock API</div></div><div className={ui.btnRow}><button className={ui.outlineBtn} onClick={()=>alert('Mock API: declined.')}>Decline</button><button className={ui.goldBtn} onClick={()=>alert('Mock API: connection accepted.')}>Accept</button></div></div></div>)}</>:<><h2 style={{fontSize:19}}>Connected people</h2>{lawyers.slice(0,3).map(l=><div key={l.slug} className={ui.listItem}><div className={ui.row}><div><b>{l.name}</b><div className={ui.muted} style={{fontSize:11}}>{l.area} · {l.location}</div></div><span className={ui.stars}>★★★</span></div></div>)}</>}</div>}</div></LafreShell>}
