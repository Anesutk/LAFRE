'use client';
import LafreShell from '../prototype/components/LafreShell';
import ui from '../prototype/components/ui.module.css';
export default function AdminPreview(){return <LafreShell role="admin" active="Home"><div className={ui.shell}><div className={ui.eyebrow}>Prototype only</div><h1 className={ui.title}>LAFRE administration.</h1><p className={ui.subtitle}>This preview shows how the existing admin role can evolve into the trust, verification and moderation layer for the new platform. The real admin pages remain untouched by this prototype.</p><div className={ui.threeCol} style={{marginTop:22}}>{[['Lawyer verification','4 pending','Review professional verification records.'],['Forum moderation','7 reports','Review reported posts and comments.'],['Users','1,284 active','Manage civilian and student accounts.'],['Legal-help requests','23 open','Monitor request activity and outcomes.'],['Mentorship','9 programmes','Manage lawyer-led programmes.'],['Platform settings','12 changes','Configure categories and trust rules.']].map(x=><div key={x[0]} className={ui.panel+' '+ui.pad}><div className={ui.eyebrow}>{x[0]}</div><h2 style={{fontSize:24,margin:'7px 0'}}>{x[1]}</h2><p className={ui.muted} style={{fontSize:12,lineHeight:1.5}}>{x[2]}</p><button className={ui.outlineBtn} onClick={()=>alert('Mock API: admin section opened.')}>Open</button></div>)}</div><section className={ui.panel+' '+ui.pad} style={{marginTop:18}}><h2 style={{fontSize:19}}>Trust & safety principle</h2><p className={ui.muted} style={{fontSize:13,lineHeight:1.7}}>The frontend can hide or show role-specific controls, but production permissions must be enforced by the Django backend. Lawyer verification, student-only questions, private messages, reports and account status changes should never depend on frontend visibility alone.</p></section></div></LafreShell>}
