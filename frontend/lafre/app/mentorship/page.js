'use client';
import Link from 'next/link';
import LafreShell from '../prototype/components/LafreShell';
import {mentorship} from '../prototype/mock-api';
import ui from '../prototype/components/ui.module.css';
export default function Mentorship(){return <LafreShell active="Mentorship" role="student"><div className={ui.shell}><div className={ui.eyebrow}>Private professional development</div><h1 className={ui.title}>Mentorship</h1><p className={ui.subtitle}>Discover programmes led by verified lawyers. Mentorship is focused on career and professional development, not personal legal representation.</p><div className={ui.row} style={{margin:'24px 0 13px'}}><h2 style={{fontSize:20}}>Programmes</h2><button className={ui.outlineBtn}>Filter</button></div><div className={ui.twoCol}>{<section className={ui.panel+' '+ui.pad}>{mentorship.map(m=><div className={ui.listItem} key={m.id}><div className={ui.row}><div><span className={ui.tag}>{m.area}</span><h3 style={{fontSize:18,margin:'8px 0 4px'}}>{m.title}</h3><div className={ui.muted} style={{fontSize:12}}>with {m.mentor} · ★★★ Verified lawyer</div></div><span style={{fontWeight:850,fontSize:12}}>{m.students} students</span></div><p style={{fontSize:13,lineHeight:1.55,color:'#5d5952'}}>{m.description}</p><div className={ui.row}><span className={ui.muted} style={{fontSize:11}}>{m.weeks} weeks · private group</span><Link href={`/mentorship/${m.id}`} className={ui.goldBtn}>View programme</Link></div></div>)}</section>}<aside className={ui.panel+' '+ui.pad}><div className={ui.eyebrow}>For verified lawyers</div><h3>Create a programme</h3><p className={ui.muted} style={{fontSize:13,lineHeight:1.6}}>Verified lawyers can offer structured programmes and select participating students.</p><button className={ui.outlineBtn} onClick={()=>alert('Mock API: mentor programme flow opened.')}>Create programme</button></aside></div></div></LafreShell>}
