'use client';
import {useState} from 'react';
import Link from 'next/link';
import LafreShell from '../../prototype/components/LafreShell';
import {mentorship} from '../../prototype/mock-api';
import ui from '../../prototype/components/ui.module.css';
export default function MentorshipDetail({params}){const m=mentorship.find(x=>x.id===params.id)||mentorship[0];const [joined,setJoined]=useState(false);return <LafreShell active="Mentorship" role="student"><div className={ui.shell}><Link href="/mentorship" className={ui.muted} style={{textDecoration:'none',fontWeight:800}}>← Mentorship</Link><div className={ui.panel+' '+ui.pad} style={{marginTop:15}}><span className={ui.tag}>{m.area}</span><h1 style={{fontSize:30,margin:'12px 0 6px'}}>{m.title}</h1><div className={ui.stars}>★★★ <span style={{color:'#666',letterSpacing:0}}>Verified lawyer mentor · {m.mentor}</span></div><p className={ui.subtitle}>{m.description}</p><div className={ui.btnRow}><button className={ui.goldBtn} onClick={()=>setJoined(true)}>{joined?'Joined (demo)':'Join private programme'}</button><button className={ui.outlineBtn}>Ask mentor</button></div></div><div className={ui.threeCol} style={{marginTop:16}}>{['Week 1 — Finding your practice direction','Week 2 — CVs, interviews and professional presence','Week 3 — Client work, ethics and law-firm life'].map(x=><div key={x} className={ui.panel+' '+ui.pad}><div className={ui.eyebrow}>Programme outline</div><h3 style={{fontSize:16}}>{x}</h3><p className={ui.muted} style={{fontSize:12}}>Private session · materials · discussion</p></div>)}</div></div></LafreShell>}
